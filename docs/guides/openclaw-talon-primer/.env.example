# Copy to .env and set at least OPENAI_API_KEY for the gateway to forward OpenClaw traffic to OpenAI.
#
# OPENAI_API_KEY=sk-your-openai-key
#
# Optional: for production, set explicit crypto keys (otherwise Talon derives from data dir).
# TALON_SECRETS_KEY=  # 64 hex chars (256-bit) for vault encryption
# TALON_SIGNING_KEY=  # 64+ hex chars for evidence HMAC
#
# Optional: second provider for OpenClaw (if gateway allows Anthropic).
# ANTHROPIC_API_KEY=sk-ant-...
#
# Optional: host port for Talon (default 8080).
# TALON_PORT=8080

OPENAI_API_KEY=
