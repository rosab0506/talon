# Required: your real OpenAI API key. Talon stores it in the vault and uses it when
# forwarding requests from OpenClaw; OpenClaw never sees this key.
OPENAI_API_KEY=

# Optional: host port (default 8080).
# TALON_PORT=8080

# Optional: for production, set explicit crypto keys.
# TALON_SECRETS_KEY=  # 64 hex chars
# TALON_SIGNING_KEY=  # 64+ hex chars
